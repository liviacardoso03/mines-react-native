import {Dimensions} from 'react-native';

const params = {
  blockSize: 30,
  borderSize: 5,
  fontSize: 15,
  headerRatio: 0.15, //15% da  tela vai representar o cabeçalho - Proporção do painel superior da tela
  difficultLevel: 0.1, //O nível de dificuldade será 1% em cima da quantidade de campos que estão na tela
};
